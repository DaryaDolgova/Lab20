using namespace std;

#include <string>
#include <iostream>



int main() 
{
	string s;
	int k , i;

	getline(cin, s);
	k =0;
	for (i = 0; i <= s.size(); i++) 
	{
		if ('A' <= s[i] && 'Z' >= s[i] ) { k++; }
	}

	cout << k << endl;

	return 0;
}
